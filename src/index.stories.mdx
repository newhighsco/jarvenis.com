import { ColorItem, ColorPalette, Meta } from '@storybook/addon-docs/blocks'
import colors from './styles/colors'

<Meta title="Intro/Branding" />

# Colors

<ColorPalette>
  {colors.map(color => (
    <ColorItem key={color.key} title={color.name} colors={color.values} />
  ))}
</ColorPalette>
